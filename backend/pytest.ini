[pytest]
# pytest configuration for LemonCheck backend tests
#
# asyncio_mode = auto: all async test functions are treated as async without
# needing the @pytest.mark.asyncio decorator on each one.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
