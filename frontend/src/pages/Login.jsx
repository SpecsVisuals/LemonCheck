/**
 * Login.jsx
 *
 * Magic link authentication page powered by Supabase Auth.
 * User enters their email → receives a magic link → session is established.
 * No password required. Redirects to Home after successful login.
 * Also handles the auth callback from Supabase after link click.
 */

export default function Login() {
  return <div>Login — coming soon</div>;
}
