/**
 * ComparableListings.jsx
 *
 * Renders a 3-up grid of comparable vehicle listings sourced by the MCP search_comps tool.
 * Each card shows: title, price, mileage, and delta vs. the analyzed listing (+/- $X).
 * Links out to the original listing URL.
 */

export default function ComparableListings({ comps }) {
  return <div>ComparableListings — coming soon</div>;
}
