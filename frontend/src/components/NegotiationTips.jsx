/**
 * NegotiationTips.jsx
 *
 * Expandable section displaying Claude's negotiation talking points.
 * Rendered as a numbered list derived from the DealReport.negotiation_points array.
 * Collapsed by default on mobile to keep the above-fold clean.
 */

export default function NegotiationTips({ points }) {
  return <div>NegotiationTips — coming soon</div>;
}
